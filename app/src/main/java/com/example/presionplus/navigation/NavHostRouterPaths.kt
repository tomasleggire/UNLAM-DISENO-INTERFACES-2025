package com.example.presionplus.navigation

enum class NavHostRouterPaths(
    val route: String,
) {
    DASHBOARD("dashboard"),
    WELCOME("welcome"),
    CALIBRATION("calibration"),
    RECORDARY("recordary")
}
